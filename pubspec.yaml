name: layouts
description: A Collection of Flutter layouts and major views widgets
version: 0.0.2
publish_to: 'none'
homepage: https://github.com/RagehAz/layouts.git
# --------------------------------------------------------------------------------
environment:
  sdk: ">=2.10.5 <3.0.0"
  flutter: ">=1.17.0"
# --------------------------------------------------------------------------------
dependencies:
  flutter:
    sdk: flutter
  # ------------------------------------
  #

  # ---------------------------
  scale:
    git:
      url: https://github.com/RagehAz/scale.git
  widget_fader:
    git:
      url: https://github.com/RagehAz/widget_fader.git
# --------------------------------------------------------------------------------
dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.0.0

flutter:
